package SCD2_2021_Exam.controller;

/**
 * Please note that although this interface is called Controller, it most suitably follows a Presenter approach
 * However it is called so because it makes sense to me, as it also controls some of the behaviour for view and model :)
 * */
public interface Controller {
    /**
     * Simple initialiser to be implemented in the subclasses.
     */
    void initController();
}
